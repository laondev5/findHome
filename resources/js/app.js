import './bootstrap';
import { Datepicker, Input, initTE, Alert, } from "tw-elements";
initTE({ Datepicker, Input, Alert, });


import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
